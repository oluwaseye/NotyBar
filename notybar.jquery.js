(function ($) { //an IIFE so safely alias jQuery to $
   

    $.NotyBar.defaultOptions = {
        position: 'left',
        blur: false,
        overlay: true,
        logs: false
    };

}(jQuery));

